### TO DO:
(Currently, at every run, new aes key is created)
1. Create a function to check if credentials are already created inside ./data;
2. Create a function to store created credentials inside ./data